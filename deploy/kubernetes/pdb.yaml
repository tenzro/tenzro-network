apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: tenzro-validator-pdb
  namespace: tenzro-testnet
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: tenzro
      component: validator
