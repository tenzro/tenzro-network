# rsync exclude list for subdir mirrors (tenzro-cli, tenzro-sdk-rust,
# tenzro-sdk-typescript, TenzroClaw, tenzro-mcp-server, tenzro-a2a-server).
#
# Differs from rsync-excludes-root.txt: also excludes .gitignore and .github/
# so each subdir mirror preserves its own copies of those.
#
# Invocation:
#   rsync -a \
#     --exclude-from=tools/sync/rsync-excludes-sub.txt \
#     /Users/hilarl/AI/tenzronetwork/<src>/ \
#     /Users/hilarl/Documents/tenzro-github/<dst>/
#
# Do NOT pass --delete or --delete-excluded.

# Internal AI-assistant instructions and memory
CLAUDE.md
.claude/

# Git metadata + per-repo .gitignore + workflows (preserve mirror's own)
.git/
.gitignore
.github/

# Build outputs
target/
node_modules/
dist/
build/
.next/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
*.egg-info/
.venv/
venv/
.coverage

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo

# Secrets
.env
.env.*
.vercel/

# Logs
*.log

# Runtime data
data/
