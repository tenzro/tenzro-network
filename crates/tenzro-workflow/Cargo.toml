[package]
name = "tenzro-workflow"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Multi-party workflow primitive for Tenzro Network — obligations, approval gates, lifecycle, policy DSL, privacy domains"

[dependencies]
tenzro-types = { workspace = true }
tenzro-crypto = { workspace = true }
tenzro-identity = { workspace = true }
tenzro-storage = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
bincode = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
dashmap = { workspace = true }
parking_lot = { workspace = true }
chrono = { workspace = true }
async-trait = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
# `tempfile` so the multi-day workflow-hydration restart test can spin
# up a temporary RocksDB, drop the manager, and rebuild from the same
# on-disk store to verify the durability invariant.
tempfile = "3"

[lints]
workspace = true
